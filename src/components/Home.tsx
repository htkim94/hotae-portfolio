function Home(
  // { appSections }: { appSections: (HTMLElement | null)[] }
) {
  return (
    <section
      id="home"
      className="h-screen bg-slate-500 p-24"
      // ref={el => appSections[0] = el}
    >
      <div className="text-white">THIS IS HOME PAGE</div>
    </section>
  );
}

export default Home
