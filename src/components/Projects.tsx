function Projects(
  // { appSections }: { appSections: (HTMLElement | null)[] }
) {
  return (
    <section
      id="projects"
      className="h-screen bg-red-500 p-24"
      // ref={el => appSections[2] = el}
    >
      <div className="text-white">THIS IS Projects PAGE</div>
    </section>
  );
}

export default Projects
