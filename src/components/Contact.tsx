function Contact(
  // { appSections }: { appSections: (HTMLElement | null)[] }
) {
  return (
    <section
      id="contact"
      className="h-screen bg-yellow-500 p-24"
      // ref={el => appSections[3] = el}
    >
      <div className="text-white">THIS IS Contact PAGE</div>
    </section>
  );
}

export default Contact
